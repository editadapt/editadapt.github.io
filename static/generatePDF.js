// create some node script that launch pagedjs on all index found in the json export from hugo. That would be the missing link. And then we have a full hugo exporter (how cool is that!)

const page = require('pagedjs-cli');

